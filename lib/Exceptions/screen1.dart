import 'package:flutter/material.dart';

class screen1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(16.0),
      child: Text(
        "1Dealing with exceptions \n\n"
            "1. Checked exceptions need to be specified or handled with a try-catch block \n\n"
            "2. Unchecked exceptions can be addressed by fixing bugs in the code or by setting a try-catch block \n\n"
            "3. Errors may have several causes, each with their own solutions"
        "2Dealing with exceptions \n\n"
            "1. Checked exceptions need to be specified or handled with a try-catch block \n\n"
            "2. Unchecked exceptions can be addressed by fixing bugs in the code or by setting a try-catch block \n\n"
            "3. Errors may have several causes, each with their own solutions"
        "3Dealing with exceptions \n\n"
            "1. Checked exceptions need to be specified or handled with a try-catch block \n\n"
            "2. Unchecked exceptions can be addressed by fixing bugs in the code or by setting a try-catch block \n\n"
            "3. Errors may have several causes, each with their own solutions"
        "4Dealing with exceptions \n\n"
            "1. Checked exceptions need to be specified or handled with a try-catch block \n\n"
            "2. Unchecked exceptions can be addressed by fixing bugs in the code or by setting a try-catch block \n\n"
            "3. Errors may have several causes, each with their own solutions",
        style: TextStyle(fontSize: 18.0),
      ),
    );
  }
}
